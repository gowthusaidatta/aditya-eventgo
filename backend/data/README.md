# EventGo Data Layer (Single Table)

This folder provides key builders and item constructors for the single-table design.

- keys.js: canonical PK/SK and GSI key builders
- access.js: item factory helpers

Use these helpers when refactoring the backend to the EventGo_Main table.
